# src/CMakeLists.txt
# Add your main library or executable targets here once sources exist.
# e.g. add_library(winsosmlow_core STATIC main.cpp)
#      target_include_directories(...) to expose headers.
